# Markdown File Previewer

A simple web-based tool that lets you preview Markdown files rendered as HTML directly in your browser.

## Features

- Load and preview `.md` files from your local machine
- Real-time rendering of Markdown to HTML
- Clean, readable output

## Usage

Open `index.html` in your browser and select a Markdown file to preview.
